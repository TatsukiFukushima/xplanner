<%= form_for(current_user.active_block_relationships.find_by(blocked_id: @user.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "ブロック中", class: "btn btn-blocking" %>
<% end %>