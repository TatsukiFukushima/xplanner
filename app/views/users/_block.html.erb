<%= form_for(current_user.active_block_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :blocked_id, @user.id %></div>
  <%= f.submit "このユーザーをブロックする", class: "btn btn-block" %>
<% end %>